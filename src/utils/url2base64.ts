// url图片 转成base64码,用于保存md的图片
const url2base64 = (fileUrl: string) => {
  // var reader = new FileReader()
  // const imgUrlBase64 = reader.readAsDataURL(fileUrl)
  // reader.onload = function (e) {
  //   console.log(reader.result)
  // }
}
export default url2base64
